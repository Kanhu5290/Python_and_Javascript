#include<stdio.h>
int main ()
{
    int i; //declare variable i
    for(i=0;i<10;i++) //loop 10 times
    {
        puts("hello world \n");//put the string to the output
    }
    return 0; // tell the os code exited without errors
}
// it's binary source code is a.out
